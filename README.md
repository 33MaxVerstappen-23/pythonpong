Please install pygame before you try this game out with this command in your powershell or terminal so that it runs without errors: python3 -m pip install pygame

I would recommend installing homebrew too using this command in your terminal (in mac only): brew install sdl2 sdl2_image sdl2_mixer sdl2_ttf pkg-configportmidi

Also Test it using this command: in your terminal or powershell: python3 -m pygame.examples.aliens

If that works then you can run the game smoothly

ABOUT THE GAME:

The game is very simple, hit the ball to the other side without the opponent touching the ball. A cool feature is that when you press the spacebar
